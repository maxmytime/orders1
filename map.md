### Поле адрес
- Функция добавления новго адреса ```addNewAddres()```

### Валидация графика платежей
- Функция валидации графика платежей ```validationSchedule()```

Добавлен новый маршрут проверки логина и пароля, добавлено суммирование полей в сделке, добавлено автоподстановка остатка суммы процентов в новую в строку в ГП, добавлено обязательная проверка ГП при разблокеровке заявки

1687 1821 4186

                        function trimSpace(str) {
                            let newStr = '';

                            for (let i = 0; i > str.length; i++) {
                                if (str[i] != ' ') {
                                    newStr += str[i];
                                }
                            }


                            return newStr;
                        }
